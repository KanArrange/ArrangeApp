package com.chart.provider;

import android.content.Context;

import com.chart.view.Chart;

/**
 * Created by kan212 on 17/11/3.
 * Default axes renderer. Can draw maximum four axes - two horizontal(top/bottom) and two vertical(left/right).
 * 默认轴渲染。最多可绘制四个轴-两个水平（上/下）和两个垂直（左/右）。
 */

public class AxesRenderer {

    public AxesRenderer(Context context, Chart chart){

    }
}
